Jakub Dragosz 22.04.2024

Program cw5a implementuje problem producenta i konsumenta z wykorzystaniem potoków nazwanych, wywołuje on forka, za pomocą forka,
gdzie proces macierzysty to serwer i odczytuje on pliki z pierwszego parametru, a proces potomny to klient i 
zapisuje on pliki do drugiego parametru wywołania.

Program cw5b implementuje problem producenta i konsumenta z wykorzystaniem potoków nazwanych, wywołuje on forka, a w środku wywołuje
execlp aby uruchomic program child.c oraz parent.c, analogicznie działa do programu cw5a

Jak skompilować programy:
=========================
Aby skompilować programy, bez wywoływanie należy użyć funkcji:
    make all


Aby skompilować i uruchomic cw5a.c:
    make run_cw5a

Aby skompilować i uruchomic cw5b.c:
    make run_cw5b

Aby wyczyscic zawartosc katalogu (usunac zbedne pliki), nalezy wykonac:
    make clean

W celu archiwizacji oraz kompresji katalogu nalezy wykonac:
    make tar
