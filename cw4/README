Jakub Dragosz 15.04.2024

Program cw4 implementuje problem producenta i konsumenta z wykorzystaniem potoków nienazwanych, wywołuje on forka i jako
serwer jest proces macierzysty, a jako klient jest proces potomny. Serwer czyta z potoku nienazwanego i zapisuje do niego, 
a później serwer zapisuje to do pliku podanego jako argument funkcji. Jako argumenty przyjmuje z jakiego pliku ma czytać tekst
i do jakiego ma zapisywać, pod koniec porównuje te 2 pliki

Jak skompilować programy:
=========================
Aby skompilować programy, bez wywoływanie należy użyć funkcji:
    make all


Aby skompilować i uruchomic cw4.c:
    make run_cw4

Aby wyczyscic zawartosc katalogu (usunac zbedne pliki), nalezy wykonac:
    make clean

W celu archiwizacji oraz kompresji katalogu nalezy wykonac:
    make tar
